# Files to ignore
.travis.yml
test.js

# Folders to ignore
examples

# Files and folders generated by tests and other commands
.nyc_output

# In case you add a node_modules folder to a subdirectory. These will not show
# up on `git status` because of the .gitignore file, but they will be inluded
# in the npm package!
**/node_modules
